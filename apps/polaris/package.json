{
  "name": "polaris",
  "version": "1.0.0",
  "description": "Service to update a Route53 record with a home network's IP.",
  "scripts": {
    "start": "node src/main.js",
    "kill": "sudo kill $(cat pid)",
    "test": "mocha tests/tests.js"
  },
  "author": "Chris Lewis",
  "devDependencies": {
    "@chris-lewis/eslint-config": "^1.0.3",
    "chai": "^4.1.2",
    "mocha": "^8.2.1"
  },
  "eslintConfig": {
    "extends": "@chris-lewis/eslint-config"
  },
  "dependencies": {
    "aws-sdk": "^2.1092.0"
  }
}
