/** Command delay time */
const DELAY_MS = 3000;

/** Key for list of users */
const ATTIC_KEY_USERS = 'users';

/** Key for fleet list */
const ATTIC_KEY_FLEETLIST = 'fleetList';

module.exports = {
  DELAY_MS,
  ATTIC_KEY_USERS,
  ATTIC_KEY_FLEETLIST,
};
