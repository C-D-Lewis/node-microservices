{
  "name": "monitor",
  "version": "1.0.0",
  "author": "Chris Lewis",
  "scripts": {
    "start": "node src/main.js",
    "deploy": "ssh pi@$MONITOR_PI \"cd /home/pi/monitor && git pull origin master && sudo reboot\"",
    "kill": "sudo kill $(cat pid)",
    "test": "mocha tests/tests.js"
  },
  "dependencies": {
    "@chris-lewis/node-common": "^1.2.2",
    "async": "^2.4.1",
    "express": "^4.15.0",
    "mocha": "^5.0.1",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
