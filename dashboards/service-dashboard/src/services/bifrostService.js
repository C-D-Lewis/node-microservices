const BifrostService = {};

/**
 * Send a packet via bifrost.
 *
 * @param {*} state 
 * @param {*} packet 
 * @param {*} tokenOverride 
 */
BifrostService.sendPacket = async (state, packet, tokenOverride) => {

};
