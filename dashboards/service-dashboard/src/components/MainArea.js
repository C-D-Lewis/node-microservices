/**
 * MainArea component.
 *
 * @returns {HTMLElement}
 */
// eslint-disable-next-line no-unused-vars
const MainArea = () => fab.Row()
  .withStyles({
    flex: 5,
    flexWrap: 'wrap',
    width: '100%',
    height: '100%',
    alignItems: 'flex-start',
  });
