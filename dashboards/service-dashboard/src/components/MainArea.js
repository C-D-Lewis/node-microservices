/**
 * MainArea component.
 *
 * @returns {HTMLElement}
 */
const MainArea = () =>
  fab.Row()
    .withStyle({
    flex: 5,
    flexWrap: 'wrap',
    width: '100%',
    height: '100%',
    alignItems: 'flex-start',
  });
