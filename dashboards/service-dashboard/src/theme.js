// eslint-disable-next-line no-unused-vars
const Colors = {
  primary: '#007bff',
  primaryDark: '#002da0',
  lightGrey: '#eee',
  darkGrey: '#bbb',
  veryDarkGrey: '#666',
  statusOk: '#43D047',
  statusDown: '#DD6E40',
};

// eslint-disable-next-line no-unused-vars
const Fonts = {
  body: 'sans-serif',
  code: 'monospace',
};
